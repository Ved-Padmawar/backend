version: 1
backend:
  phases:
    build:
      commands:
        - pip install -r requirements.txt
frontend:
  phases:
    preBuild:
      commands:
        - echo "No frontend build required"
    build:
      commands:
        - echo "Backend only deployment"
  artifacts:
    files:
      - '**/*'